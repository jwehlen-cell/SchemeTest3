# SchemaSpy Oracle configuration (optional convenience file)
# You can run: java -jar aws/schemaspy.jar -configFile aws/schemaspy-oracle.properties
# Environment variables override these values at runtime if your wrapper script supports it.

# Database type and driver
schemaspy.t=orathin
schemaspy.dp=aws/ojdbc8.jar

# Connection
schemaspy.host=usndc-oracle-db.c0320kayk04f.us-east-1.rds.amazonaws.com
schemaspy.port=1521
# For Oracle, -db is the service name (or SID when configured accordingly)
schemaspy.db=ORCL
schemaspy.cat=%

# Authentication and schema
schemaspy.u=oracleadmin
schemaspy.p=OracleTest2024!
schemaspy.s=ORACLEADMIN

# Output
schemaspy.o=deploy-live/docs/oracle

# Rendering
# Use -vizjs if Java >=11 and Graphviz is not installed; otherwise prefer Graphviz 'dot'
# To pass tuning args to Graphviz, set env var GRAPHVIZ_ARGS before running your script.
# Example: export GRAPHVIZ_ARGS="-Gfontsize=10 -Nfontsize=9 -Efontsize=8 -Gnodesep=0.35 -Granksep=0.55"
# schemaspy.vizjs=true

# Misc
schemaspy.degree=2
schemaspy.hq=true
schemaspy.norows=true
schemaspy.noimplied=true
schemaspy.desc=USNDC Oracle operational schema (RDS 19c) – 177 tables, 101 foreign keys.
